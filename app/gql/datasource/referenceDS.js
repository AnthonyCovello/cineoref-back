const { SQLDataSource } = require('datasource-sql');
const DataLoader = require("dataloader");

const MINUTE = 60;
const HEURE = 3600;

class Reference extends SQLDataSource {
    
}

module.exports= Reference;